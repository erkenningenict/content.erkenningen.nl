import React from 'react';
import './LinkButtonContainer.scss';

const LinkButtonContainer = (props) => {
  return <div className="LinkButtonContainer">{props.children}</div>;
};

export default LinkButtonContainer;
